# Automation / detection / analysis tooling (the defensive side).
# Kept light so it installs fast on a low-spec host.
duckdb==1.1.3
PyYAML==6.0.2
networkx==3.4.2
Jinja2==3.1.5
requests==2.32.3
pytest==8.3.4
tabulate==0.9.0
Pillow==11.0.0
